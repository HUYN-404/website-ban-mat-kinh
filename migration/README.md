# Migration Workspace

This folder contains implementation artifacts for DRF strangler migration.

- `contracts/`: frozen v1 API contract
- `gateway/`: Nginx gateway config and rollout notes
- `tryon/`: virtual try-on design notes
- `validation/`: contract smoke and shadow compare scripts
- `decommission/`: Node cutover checklist
